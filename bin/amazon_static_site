#!/usr/bin/env ruby
require_relative "../lib/amazon_static_site.rb"
require "pry"

if ARGV[0].to_s =~ /^(serve|s)$/i
  AmazonStaticSite::Server.new.start
else
  AmazonStaticSite::Worker.new(file: ARGV[0], folder: ARGV[1])
end